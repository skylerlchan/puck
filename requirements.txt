slack-bolt>=1.18
slack-sdk>=3.27
pyyaml>=6.0
# External CLIs (not pip): `claude` (Claude Code) and `gh` (GitHub CLI), both
# already authenticated on the worker. Optional: Playwright for before/after video.
